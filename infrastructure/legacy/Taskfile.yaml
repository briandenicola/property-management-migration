version: '3'

vars:
  DEFAULT_REGION: southcentralus

tasks:
  up:
    desc: Creates the legacy infrastructure in Azure
    cmds:
    - task: init
    - task: apply

  init:
    desc: Initializes Terraform
    cmds:
    - terraform workspace new {{.REGION}} || true
    - terraform workspace select {{.REGION}}
    - terraform init
    vars:
      REGION: '{{default .DEFAULT_REGION .CLI_ARGS}}'

  apply:
    desc: Applies Terraform configuration
    cmds:
    - terraform apply -auto-approve -var "location={{.REGION}}" -compact-warnings
    vars:
      REGION: '{{default .DEFAULT_REGION .CLI_ARGS}}'

  plan:
    desc: Shows Terraform execution plan
    cmds:
    - terraform plan -var "location={{.REGION}}" -compact-warnings
    vars:
      REGION: '{{default .DEFAULT_REGION .CLI_ARGS}}'

  creds:
    desc: Shows VM and domain credentials from Terraform output
    cmds:
    - cmd: echo "Domain - {{.DOMAIN}}"
      silent: true
    - cmd: echo "Admin UPN - {{.ADMIN_UPN}}"
      silent: true
    - cmd: echo "Local Admin - {{.USERNAME}} / {{.PASSWORD}}"
      silent: true
    - cmd: echo "SQL Password - {{.SQL_PASSWORD}}"
      silent: true
    vars:
      DOMAIN:
        sh: terraform output -raw domain_name
      ADMIN_UPN:
        sh: terraform output -raw domain_admin_upn
      USERNAME:
        sh: terraform output -raw vm_admin_username
      PASSWORD:
        sh: terraform output -raw vm_admin_password
      SQL_PASSWORD:
        sh: terraform output -raw sql_sa_password

  connect:
    desc: "Connect to a VM via Azure Bastion (usage: task connect -- iis|dc|client)"
    cmds:
    - az network bastion rdp --name {{.BASTION}} --resource-group {{.RG}} --target-resource-id {{.VM_ID}}
    vars:
      BASTION:
        sh: terraform output -raw bastion_name
      RG:
        sh: terraform output -raw resource_group_name
      VM_ID:
        sh: |
          {{if eq .CLI_ARGS "dc"}}terraform output -raw dc_vm_id
          {{else if eq .CLI_ARGS "client"}}terraform output -raw client_vm_id
          {{else}}terraform output -raw iis_vm_id
          {{end}}

  down:
    desc: Destroys all Azure resources and cleans up Terraform
    cmds:
    - az group delete -n {{.RG}} --yes --no-wait || true
    - powershell -Command "Remove-Item -Recurse -Force terraform.tfstate.d, .terraform.lock.hcl, .terraform, terraform.tfstate, terraform.tfstate.backup, .terraform.tfstate.lock.info -ErrorAction SilentlyContinue"
    vars:
      RG:
        sh: terraform output -raw resource_group_name
